{
  "name": "@flyrank/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reusable building blocks shared across every assignment (logging, config, types, helpers).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  }
}
